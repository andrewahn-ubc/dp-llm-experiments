# DELMAN deps for Compute Canada / Alliance wheelhouse (Narval, Rorqual, …).
# Do NOT use upstream DELMAN/requirements.txt (pins torch==2.4.0, missing here).
#
# If pip hits the pyarrow-noinstall dummy wheel:
#   deactivate
#   module load StdEnv/2023 gcc arrow
#   source $SCRATCH/venv/nanogcg/bin/activate
#   pip install -r experiments/delman/requirements_cc.txt
#
# Or skip datasets (only needed if DELMAN loads HF Datasets at runtime):
#   pip install -r experiments/delman/requirements_cc.txt --no-deps  # not recommended
#   better: omit datasets line and install the rest.

# Upstream DELMAN pins transformers==4.49. Newer 4.5x/5.x change Llama layer
# outputs so compute_v_star hooks see 2D tensors → IndexError on cur_out[0][i,idx,:].
transformers==4.49.0
# Alliance `module load arrow` is often pyarrow>=21, which removed PyExtensionType.
# datasets<2.19 still uses pa.PyExtensionType → AttributeError on import.
# Need datasets>=2.20 with arrow loaded *before* venv activate.
datasets>=2.20.0,<4
matplotlib>=3.5
numpy>=1.26,<2
accelerate>=0.30
protobuf>=4,<6
sentencepiece>=0.2
